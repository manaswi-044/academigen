services:
  - type: web
    name: academigen-pdf-service
    runtime: python
    buildCommand: pip install -r pdf_service/requirements.txt
    startCommand: uvicorn pdf_service.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0s